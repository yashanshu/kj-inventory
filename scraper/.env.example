# Swiggy Partner Credentials
SWIGGY_MOBILE=

# KJ Inventory API
KJ_API_URL=http://localhost:8888/api/v1
KJ_API_TOKEN=

# Notification Toggles (Default: true)
ENABLE_WHATSAPP=false
ENABLE_TELEGRAM=true
ENABLE_FCM=true

# Per-channel event whitelists (comma-separated event names)
# Restricts which event types each channel sends. Leave unset = all events allowed.
# Events: new_order, cancelled_order, overdue, order_ready, handover_delay,
#         complaint, kitchen_stress, session_expired, error, daily_summary, promo_alert
WHATSAPP_EVENTS=new_order
# TELEGRAM_EVENTS=
# FCM_EVENTS=

# WhatsApp (Unofficial)
# Format: CountryCode + Number (e.g., 919876543210)
WHATSAPP_TO_NUMBER=

# Telegram Notification
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Firebase Cloud Messaging (Android Push)
# Path to Firebase service account JSON file
FIREBASE_SERVICE_ACCOUNT_PATH=./config/firebase-service-account.json
FIREBASE_FCM_TOKEN=

# Scraper Settings
# Run browser in headless mode (default: true). Set false to see the browser.
PUPPETEER_HEADLESS=true
# Polling interval in milliseconds (default: 30000 = 30s)
POLL_INTERVAL_MS=30000
# Number of consecutive poll errors before scraper stops (default: 10)
MAX_CONSECUTIVE_ERRORS=10
# Scraper API server port (default: 3001)
API_PORT=3001

# Restaurant Operating Shifts (IST, 24-hour format)
# JSON array of shifts. Overnight shifts supported (e.g., 19-3 = 7PM to 3AM)
# Polling pauses outside these hours to save resources
# Default: [{"open":12,"close":15},{"open":19,"close":3}]
RESTAURANT_SHIFTS=[{"open":12,"close":15},{"open":19,"close":3}]
